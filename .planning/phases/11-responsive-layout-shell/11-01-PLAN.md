---
phase: 11-responsive-layout-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/editor/EditorLayout.tsx
  - src/components/editor/MobileEditorLayout.tsx
  - src/app/dashboard/page.tsx
autonomous: true

must_haves:
  truths:
    - "User on a phone (<768px) can open the editor and see a full-screen canvas instead of the 'Larger Screen Required' gate"
    - "User on a tablet (768px-1024px portrait) sees a two-column layout with palette sidebar and canvas"
    - "User on a landscape tablet (1024px+) sees the unchanged three-column desktop layout"
    - "User on desktop (>1024px) sees the unchanged three-column layout (palette, canvas, properties)"
    - "User on a phone sees a floating bottom toolbar with undo, redo, add, connect action buttons"
    - "User on a phone can browse the structure dashboard with single-column full-width cards"
    - "User on a phone sees delete buttons always visible on dashboard structure cards (no hover dependency)"
  artifacts:
    - path: "src/components/editor/MobileEditorLayout.tsx"
      provides: "Full-screen canvas layout with floating toolbar and bottom sheet mount points"
      min_lines: 80
    - path: "src/components/editor/EditorLayout.tsx"
      provides: "Breakpoint branching: mobile -> MobileEditorLayout, tablet/desktop -> existing layout"
      contains: "MobileEditorLayout"
    - path: "src/app/dashboard/page.tsx"
      provides: "Responsive dashboard with mobile-adapted header and always-visible delete on touch"
      contains: "isTouchDevice"
  key_links:
    - from: "src/components/editor/EditorLayout.tsx"
      to: "src/components/editor/MobileEditorLayout.tsx"
      via: "conditional render on isMobile"
      pattern: "isMobile.*MobileEditorLayout"
    - from: "src/components/editor/MobileEditorLayout.tsx"
      to: "src/components/canvas/Canvas.tsx"
      via: "renders Canvas component full-screen"
      pattern: "<Canvas"
    - from: "src/components/editor/MobileEditorLayout.tsx"
      to: "src/stores/ui-store.ts"
      via: "reads/writes mobile Zustand state"
      pattern: "useUIStore"
---

<objective>
Remove the mobile gate from EditorLayout, create a MobileEditorLayout with full-screen canvas and floating toolbar, wire breakpoint branching so phones get the mobile layout while tablet/desktop remain unchanged, and make the dashboard responsive for phone screens.

Purpose: This is the structural foundation that enables all subsequent mobile phases. Without it, phones see a dead-end "Larger Screen Required" message.
Output: MobileEditorLayout.tsx (new), modified EditorLayout.tsx (branching), modified dashboard/page.tsx (responsive)
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-mobile-foundation/10-01-SUMMARY.md
@src/components/editor/EditorLayout.tsx
@src/components/canvas/Canvas.tsx
@src/app/dashboard/page.tsx
@src/hooks/useDeviceCapabilities.ts
@src/stores/ui-store.ts
@src/components/mobile/BottomSheet.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove mobile gate and create MobileEditorLayout with breakpoint branching</name>
  <files>src/components/editor/EditorLayout.tsx, src/components/editor/MobileEditorLayout.tsx</files>
  <action>
**In EditorLayout.tsx:**
1. Replace `useMediaQuery` imports with `useDeviceCapabilities` from `@/hooks/useDeviceCapabilities`.
2. Delete the mobile gate block (lines 262-282) that returns "Larger Screen Required".
3. Add breakpoint branching at the top of the render: if `isMobile` (from useDeviceCapabilities), return `<MobileEditorLayout>` passing through all the same props that EditorLayoutInner receives (structureId, structureName, onSave, isSaving, canSave), plus the hooks it needs: undo, redo, canUndo, canRedo, hasDraft, restoreDraft, discardDraft, deleteConfirm state and handlers.
4. The existing tablet and desktop code path remains COMPLETELY UNCHANGED. The `isTablet` logic for auto-collapsing palette and right panel overlay stays exactly as-is. Use `isTablet` and `isDesktop` from useDeviceCapabilities instead of raw useMediaQuery -- behavior stays identical, just the source changes.
5. The template picker modal and delete confirmation dialog must render on ALL screen sizes (keep them outside the mobile/desktop branch).

**Create MobileEditorLayout.tsx:**
A new component `src/components/editor/MobileEditorLayout.tsx` that renders the phone layout:

```
Structure:
  <div className="h-[100dvh] w-screen flex flex-col overflow-hidden">
    {/* Draft restore banner (same as desktop, if hasDraft) */}
    {/* Full-screen canvas area */}
    <div className="flex-1 relative">
      <Canvas />
    </div>
    {/* Floating bottom toolbar */}
    <div className="absolute bottom-4 left-1/2 -translate-x-1/2 z-30
                    flex items-center gap-1 px-3 py-2
                    bg-white/90 dark:bg-gray-800/90 backdrop-blur-sm
                    rounded-full shadow-lg border border-gray-200 dark:border-gray-700">
      {/* Toolbar buttons: Undo, Redo, Add (opens palette), Connect */}
    </div>
    {/* Shell mount points for future bottom sheets (empty in this phase) */}
    {/* These will be populated in Phase 12 (palette) and Phase 13 (properties) */}
  </div>
```

Props for MobileEditorLayout:
- structureId, structureName (for display/context)
- onSave, isSaving, canSave (save functionality)
- onUndo, onRedo, canUndo, canRedo (undo/redo from useUndoRedo hook -- passed from parent)
- hasDraft, restoreDraft, discardDraft (draft restore -- passed from parent)
- deleteConfirm, confirmDelete, cancelDelete, deleteTitle, deleteDescription (delete dialog state -- passed from parent or render at EditorLayout level)

Floating bottom toolbar buttons (use lucide-react icons):
- Undo button (Undo2 icon, disabled when !canUndo, calls onUndo)
- Redo button (Redo2 icon, disabled when !canRedo, calls onRedo)
- Add button (Plus icon, calls setMobilePaletteOpen(true) from ui-store -- no-op in this phase, wired for Phase 12)
- Connect button (Link icon, calls setMobileTool('connect') from ui-store -- no-op in this phase, wired for Phase 14)

Button styling: each button is `p-2.5 rounded-full` with `text-gray-600 dark:text-gray-300` base, `active:bg-gray-100 dark:active:bg-gray-700` for touch feedback, `disabled:opacity-30` for unavailable actions.

Use `h-[100dvh]` (dynamic viewport height) to handle iOS Safari's address bar correctly.

The structure name display: Claude's Discretion per CONTEXT.md. Decision: Do NOT show a floating header for structure name in this phase -- the full-screen canvas needs maximum real estate. The structure name is accessible via the dashboard. This keeps the layout clean like Figma mobile.
  </action>
  <verify>
Run `npx tsc --noEmit` -- no TypeScript errors.
Run `npm run build` -- successful build.
Verify MobileEditorLayout.tsx exists and exports a component.
Verify EditorLayout.tsx no longer contains "Larger Screen Required" text.
Verify EditorLayout.tsx imports and conditionally renders MobileEditorLayout.
  </verify>
  <done>
Phone-sized screens (<768px) render MobileEditorLayout with full-screen Canvas and floating bottom toolbar. Tablet and desktop layouts are completely unchanged. Delete confirmation dialog and template picker modal render on all screen sizes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Make dashboard responsive for mobile with touch-friendly cards</name>
  <files>src/app/dashboard/page.tsx</files>
  <action>
Modify `src/app/dashboard/page.tsx` to be fully responsive on phone screens:

1. Import `useDeviceCapabilities` from `@/hooks/useDeviceCapabilities`. Extract `isMobile` and `isTouchDevice`.

2. **Header on mobile** -- stack vertically instead of horizontal flex:
   - On mobile: flex-col layout, title and subtitle stack left, "New Structure" button becomes full-width below the title
   - On desktop: keep existing `flex items-center justify-between` row layout
   - Reduce padding on mobile: `px-4 py-3` instead of `px-6 py-4`
   - Approach: Use conditional className -- `{isMobile ? 'flex flex-col gap-3 px-4 py-3' : 'flex items-center justify-between px-6 py-4'}`

3. **New Structure CTA on mobile** -- full-width button at top per locked decision:
   - On mobile: `w-full justify-center` added to the Link button
   - On desktop: existing inline button unchanged

4. **Delete button visibility** -- always visible on touch devices:
   - Current: `opacity-0 group-hover:opacity-100`
   - Change: if `isTouchDevice`, remove the opacity-0/group-hover classes so the button is always visible
   - Approach: `className={... ${isTouchDevice ? 'opacity-100' : 'opacity-0 group-hover:opacity-100'} ...}`

5. **Content area padding** -- reduce on mobile:
   - On mobile: `px-4 py-6` instead of `px-6 py-8`

6. **Grid already responsive** -- the existing `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6` handles single-column on phone automatically. Leave as-is. Reduce gap on mobile: `gap-4` on mobile vs `gap-6` on desktop.

7. **Delete confirmation dialog** -- already has `mx-4` for mobile spacing, leave unchanged.
  </action>
  <verify>
Run `npx tsc --noEmit` -- no TypeScript errors.
Run `npm run build` -- successful build.
Verify dashboard/page.tsx imports useDeviceCapabilities.
Verify the delete button has conditional opacity based on isTouchDevice.
  </verify>
  <done>
Dashboard renders with single-column full-width cards on phone, full-width "New Structure" button, stacked header, and always-visible delete buttons on touch devices. Desktop dashboard is visually unchanged.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. EditorLayout no longer contains "Larger Screen Required" text
3. MobileEditorLayout.tsx exists and renders Canvas + floating toolbar
4. Dashboard uses useDeviceCapabilities for responsive adaptations
5. Desktop EditorLayout render path is unchanged (same three-column structure)
</verification>

<success_criteria>
- Phone users see a full-screen canvas with floating bottom toolbar instead of the mobile gate
- Tablet users see the same two-column layout as before (palette sidebar, canvas, overlay properties)
- Desktop users see the exact same three-column layout as before
- Phone users see a responsive dashboard with single-column cards and full-width CTA
- Touch device users see delete buttons on dashboard cards without needing hover
</success_criteria>

<output>
After completion, create `.planning/phases/11-responsive-layout-shell/11-01-SUMMARY.md`
</output>
