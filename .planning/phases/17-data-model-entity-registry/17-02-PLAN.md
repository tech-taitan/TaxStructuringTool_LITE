---
phase: 17-data-model-entity-registry
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - src/lib/entity-registry.ts
  - src/components/canvas/Canvas.tsx
  - src/components/mobile/MobilePalette.tsx
autonomous: true

must_haves:
  truths:
    - "ENTITY_REGISTRY contains all 6 HK entity types with correct categories"
    - "ENTITY_REGISTRY contains all 7 SG entity types with correct categories"
    - "ENTITY_REGISTRY contains all 10 LU entity types with correct categories"
    - "Non-AU entity nodes display correct flag emoji from JURISDICTIONS registry lookup"
    - "Existing AU-only saved structures load and render without any changes or errors"
    - "Total registry has 54 entity types (11 AU + 9 UK + 11 US + 6 HK + 7 SG + 10 LU)"
  artifacts:
    - path: "src/lib/entity-registry.ts"
      provides: "23 new entity entries (6 HK + 7 SG + 10 LU) for a total of 54"
      contains: "'hk-private-co'"
    - path: "src/components/canvas/Canvas.tsx"
      provides: "JURISDICTIONS registry lookup replacing hardcoded AU flag ternary"
      contains: "JURISDICTIONS["
    - path: "src/components/mobile/MobilePalette.tsx"
      provides: "JURISDICTIONS registry lookup replacing hardcoded AU flag ternary"
      contains: "JURISDICTIONS["
  key_links:
    - from: "src/components/canvas/Canvas.tsx"
      to: "src/models/jurisdiction.ts"
      via: "JURISDICTIONS import for flag lookup"
      pattern: "import.*JURISDICTIONS.*from.*jurisdiction"
    - from: "src/components/mobile/MobilePalette.tsx"
      to: "src/models/jurisdiction.ts"
      via: "JURISDICTIONS import for flag lookup"
      pattern: "import.*JURISDICTIONS.*from.*jurisdiction"
    - from: "src/lib/entity-registry.ts"
      to: "src/lib/constants.ts"
      via: "COLORS.entity color references for fund/holding categories"
      pattern: "COLORS\\.entity\\.(fund|holding)"
---

<objective>
Add HK, SG, and LU entity registry entries and fix the jurisdictionFlag lookup so all jurisdictions display correct flag emojis.

Purpose: Completes the entity registry with all 43 new entity types across 5 jurisdictions and fixes the hardcoded AU flag ternary that would produce blank flags for non-AU entities.

Output: Complete 54-entry entity registry across 6 jurisdictions, with correct flag display in all entity creation paths.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-data-model-entity-registry/17-RESEARCH.md
@.planning/phases/17-data-model-entity-registry/17-01-SUMMARY.md
@src/lib/entity-registry.ts
@src/components/canvas/Canvas.tsx
@src/components/mobile/MobilePalette.tsx
@src/models/jurisdiction.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 6 HK, 7 SG, and 10 LU entity registry entries</name>
  <files>
    src/lib/entity-registry.ts
  </files>
  <action>
    Add 23 new entries to `ENTITY_REGISTRY` in `src/lib/entity-registry.ts`, after the US entries added in Plan 17-01. Group by jurisdiction: HK, then SG, then LU.

    **HK (6 entries):**
    | ID | Category | Display Name | Short Name | Shape | Icon | Default Data |
    |----|----------|-------------|------------|-------|------|-------------|
    | `hk-private-co` | company | Private Company Limited by Shares | Private Co | rectangle | building-2 | `{ brn: '', crNumber: '' }` |
    | `hk-public-co` | company | Public Company | Public Co | rectangle | building-2 | `{ brn: '', crNumber: '' }` |
    | `hk-lp` | partnership | Limited Partnership | LP | triangle | file-signature | `{ brn: '' }` |
    | `hk-lpf` | fund | Limited Partnership Fund | LPF | hexagon | landmark | `{ brn: '', fundManagerName: '' }` |
    | `hk-ofc` | fund | Open-Ended Fund Company | OFC | hexagon | landmark | `{ brn: '', fundManagerName: '' }` |
    | `hk-individual` | individual | Individual | Individual | oval | user | `{ hkid: '' }` |

    **SG (7 entries):**
    | ID | Category | Display Name | Short Name | Shape | Icon | Default Data |
    |----|----------|-------------|------------|-------|------|-------------|
    | `sg-pte-ltd` | company | Private Limited Company | Pte Ltd | rectangle | building-2 | `{ uen: '' }` |
    | `sg-public-co` | company | Public Company | Public Co | rectangle | building-2 | `{ uen: '' }` |
    | `sg-lp` | partnership | Limited Partnership | LP | triangle | file-signature | `{ uen: '' }` |
    | `sg-llp` | partnership | Limited Liability Partnership | LLP | triangle | handshake | `{ uen: '' }` |
    | `sg-vcc` | fund | Variable Capital Company | VCC | hexagon | landmark | `{ uen: '', fundManagerName: '' }` |
    | `sg-unit-trust` | trust | Unit Trust | Unit Trust | triangle | shield | `{ uen: '', trusteeName: '' }` |
    | `sg-individual` | individual | Individual | Individual | oval | user | `{ nric: '' }` |

    **LU (10 entries):**
    | ID | Category | Display Name | Short Name | Shape | Icon | Default Data |
    |----|----------|-------------|------------|-------|------|-------------|
    | `lu-sarl` | company | S.a r.l. (Private Limited) | Sarl | rectangle | building-2 | `{ rcsNumber: '' }` |
    | `lu-sa` | company | S.A. (Public Limited) | SA | rectangle | building-2 | `{ rcsNumber: '' }` |
    | `lu-scsp` | partnership | SCSp (Special Limited Partnership) | SCSp | triangle | handshake | `{ rcsNumber: '' }` |
    | `lu-scs` | partnership | SCS (Limited Partnership) | SCS | triangle | file-signature | `{ rcsNumber: '' }` |
    | `lu-sicav` | fund | SICAV (Investment Fund) | SICAV | hexagon | landmark | `{ rcsNumber: '', cssfApproval: false }` |
    | `lu-sicar` | fund | SICAR (Risk Capital Fund) | SICAR | hexagon | landmark | `{ rcsNumber: '', cssfApproval: false }` |
    | `lu-sif` | fund | SIF (Specialised Investment Fund) | SIF | hexagon | landmark | `{ rcsNumber: '', cssfApproval: false }` |
    | `lu-raif` | fund | RAIF (Reserved Alternative Fund) | RAIF | hexagon | landmark | `{ rcsNumber: '' }` |
    | `lu-soparfi` | holding | Soparfi (Holding Company) | Soparfi | diamond | layers | `{ rcsNumber: '' }` |
    | `lu-individual` | individual | Individual | Individual | oval | user | `{ nationalId: '' }` |

    Each entry follows the same `EntityTypeConfig` structure as the UK/US entries from Plan 17-01:
    - `color`: use `COLORS.entity.[category]` (e.g., `COLORS.entity.fund` for fund category, `COLORS.entity.holding` for holding)
    - `description`: one-line plain-English description (e.g., "A Hong Kong private company limited by shares, incorporated under the Companies Ordinance.")
  </action>
  <verify>
    Run `npx tsc --noEmit` — zero errors. Then verify counts:
    - `getEntitiesByJurisdiction('HK').length === 6`
    - `getEntitiesByJurisdiction('SG').length === 7`
    - `getEntitiesByJurisdiction('LU').length === 10`
    - Total: `Object.keys(ENTITY_REGISTRY).length === 54`
  </verify>
  <done>
    ENTITY_REGISTRY contains 54 entries total (11 AU + 9 UK + 11 US + 6 HK + 7 SG + 10 LU). All entries have correct category assignments matching the research entity lists. TypeScript compiles cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix jurisdictionFlag lookup in Canvas.tsx and MobilePalette.tsx</name>
  <files>
    src/components/canvas/Canvas.tsx
    src/components/mobile/MobilePalette.tsx
  </files>
  <action>
    The current code hardcodes `jurisdictionFlag: canvasJurisdiction === 'AU' ? '🇦🇺' : ''` which produces blank flags for all non-AU entities. Replace with a JURISDICTIONS registry lookup in all 3 locations.

    **1. Canvas.tsx — add import** at top of file:
    Add `import { JURISDICTIONS, type Jurisdiction } from '@/models/jurisdiction';` (if not already imported).

    **2. Canvas.tsx — fix line ~225** (onDrop handler, entity creation from palette drag):
    Replace:
    ```
    jurisdictionFlag: canvasJurisdiction === 'AU' ? '\u{1F1E6}\u{1F1FA}' : '',
    ```
    With:
    ```
    jurisdictionFlag: JURISDICTIONS[canvasJurisdiction as Jurisdiction]?.flag ?? '',
    ```

    **3. Canvas.tsx — fix line ~370** (onConnect handler, entity creation from connection):
    Replace the same ternary pattern with the same JURISDICTIONS lookup.

    **4. MobilePalette.tsx — add import** at top of file:
    Add `import { JURISDICTIONS, type Jurisdiction } from '@/models/jurisdiction';` (if not already imported).

    **5. MobilePalette.tsx — fix line ~65** (handleSelect, entity creation from tap):
    Replace:
    ```
    jurisdictionFlag: canvasJurisdiction === 'AU' ? '\u{1F1E6}\u{1F1FA}' : '',
    ```
    With:
    ```
    jurisdictionFlag: JURISDICTIONS[canvasJurisdiction as Jurisdiction]?.flag ?? '',
    ```

    **Important:** Do NOT change any other logic in these files. The `canvasJurisdiction` variable and entity creation flow remain the same — only the flag resolution changes from a hardcoded ternary to a registry lookup.
  </action>
  <verify>
    Run `npx tsc --noEmit` — zero errors. Search the codebase for the old pattern `=== 'AU' ? '` to confirm no hardcoded flag ternaries remain in entity creation paths (Canvas.tsx, MobilePalette.tsx).
  </verify>
  <done>
    All 3 hardcoded jurisdictionFlag ternaries replaced with `JURISDICTIONS[canvasJurisdiction as Jurisdiction]?.flag ?? ''`. Any entity created from any jurisdiction gets the correct flag emoji from the JURISDICTIONS registry. TypeScript compiles cleanly.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `getEntitiesByJurisdiction('HK').length === 6`
3. `getEntitiesByJurisdiction('SG').length === 7`
4. `getEntitiesByJurisdiction('LU').length === 10`
5. `Object.keys(ENTITY_REGISTRY).length === 54`
6. No `=== 'AU' ? '\u{1F1E6}\u{1F1FA}'` patterns remain in Canvas.tsx or MobilePalette.tsx
7. `dev` server starts and existing AU-only saved structures load without errors (backward compatibility — the snapshot schema uses `.passthrough()` and `z.string().min(2)` for jurisdiction so no schema changes needed)
</verification>

<success_criteria>
- TypeScript compiles with zero errors
- ENTITY_REGISTRY has 54 total entries across 6 jurisdictions (11+9+11+6+7+10)
- All HK, SG, LU entity counts match success criteria exactly
- JurisdictionFlag lookup uses JURISDICTIONS registry in all entity creation paths
- No hardcoded AU flag ternaries remain
- Existing AU saved structures continue to load (backward compatibility)
</success_criteria>

<output>
After completion, create `.planning/phases/17-data-model-entity-registry/17-02-SUMMARY.md`
</output>
