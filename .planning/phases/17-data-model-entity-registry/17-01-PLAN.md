---
phase: 17-data-model-entity-registry
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/models/jurisdiction.ts
  - src/models/entities.ts
  - src/lib/constants.ts
  - src/lib/entity-registry.ts
  - src/lib/palette-icons.ts
autonomous: true

must_haves:
  truths:
    - "Jurisdiction type accepts 'AU' | 'UK' | 'US' | 'HK' | 'SG' | 'LU'"
    - "JURISDICTIONS registry has config entries for all 6 jurisdictions with correct flag, currency, and tax authority"
    - "EntityCategory type includes 'fund', 'holding', and 'pension' alongside existing 6 values"
    - "COLORS.entity and COLORS.entityBg have entries for fund, holding, and pension categories"
    - "CATEGORY_CONFIG has 9 entries covering all categories with correct labels and icons"
    - "PALETTE_ICONS has entries for 'layers' and 'piggy-bank'"
    - "ENTITY_REGISTRY contains all 9 UK entity types with correct categories"
    - "ENTITY_REGISTRY contains all 11 US entity types with correct categories"
  artifacts:
    - path: "src/models/jurisdiction.ts"
      provides: "Expanded Jurisdiction union type and 6-entry JURISDICTIONS registry"
      contains: "'AU' | 'UK' | 'US' | 'HK' | 'SG' | 'LU'"
    - path: "src/models/entities.ts"
      provides: "Expanded EntityCategory union with fund, holding, pension"
      contains: "'fund'"
    - path: "src/lib/constants.ts"
      provides: "Color entries for 3 new entity categories"
      contains: "fund:"
    - path: "src/lib/entity-registry.ts"
      provides: "20 new entity entries (9 UK + 11 US) and 3 new CATEGORY_CONFIG entries"
      contains: "'uk-ltd'"
    - path: "src/lib/palette-icons.ts"
      provides: "Layers and PiggyBank icon mappings"
      contains: "'layers'"
  key_links:
    - from: "src/lib/entity-registry.ts"
      to: "src/models/entities.ts"
      via: "EntityCategory type import"
      pattern: "category: 'fund'"
    - from: "src/lib/entity-registry.ts"
      to: "src/lib/constants.ts"
      via: "COLORS.entity color references"
      pattern: "COLORS\\.entity\\.(fund|holding|pension)"
    - from: "src/lib/entity-registry.ts"
      to: "src/models/jurisdiction.ts"
      via: "jurisdiction field values"
      pattern: "jurisdiction: '(UK|US)'"
---

<objective>
Expand the type system foundation and add UK and US entity registry entries.

Purpose: The Jurisdiction type, EntityCategory type, COLORS, CATEGORY_CONFIG, and PALETTE_ICONS must all be expanded BEFORE any non-AU entity entries can be added. This plan does that foundational work and then adds the first 20 entities (UK and US), validating the pipeline end-to-end.

Output: Expanded type system with 6 jurisdictions, 9 entity categories, and 20 new entity registry entries for UK and US.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-data-model-entity-registry/17-RESEARCH.md
@src/models/jurisdiction.ts
@src/models/entities.ts
@src/lib/constants.ts
@src/lib/entity-registry.ts
@src/lib/palette-icons.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand type system — Jurisdiction, EntityCategory, COLORS, CATEGORY_CONFIG, PALETTE_ICONS</name>
  <files>
    src/models/jurisdiction.ts
    src/models/entities.ts
    src/lib/constants.ts
    src/lib/entity-registry.ts
    src/lib/palette-icons.ts
  </files>
  <action>
    **1. Expand Jurisdiction type and JURISDICTIONS registry** in `src/models/jurisdiction.ts`:
    - Change `export type Jurisdiction = 'AU';` to `export type Jurisdiction = 'AU' | 'UK' | 'US' | 'HK' | 'SG' | 'LU';`
    - Update the file comment to reflect multi-jurisdiction support (remove "Starts with Australia" phrasing)
    - Add 5 new entries to the `JURISDICTIONS` record:
      ```
      UK: { code: 'UK', name: 'United Kingdom', flag: '🇬🇧', currencyCode: 'GBP', taxAuthorityName: 'HM Revenue & Customs' }
      US: { code: 'US', name: 'United States', flag: '🇺🇸', currencyCode: 'USD', taxAuthorityName: 'Internal Revenue Service' }
      HK: { code: 'HK', name: 'Hong Kong', flag: '🇭🇰', currencyCode: 'HKD', taxAuthorityName: 'Inland Revenue Department' }
      SG: { code: 'SG', name: 'Singapore', flag: '🇸🇬', currencyCode: 'SGD', taxAuthorityName: 'Inland Revenue Authority of Singapore' }
      LU: { code: 'LU', name: 'Luxembourg', flag: '🇱🇺', currencyCode: 'EUR', taxAuthorityName: 'Administration des Contributions Directes' }
      ```
    - Use Unicode escape sequences for flag emojis (e.g., `'\u{1F1EC}\u{1F1E7}'` for UK) to match existing AU pattern.

    **2. Expand EntityCategory type** in `src/models/entities.ts`:
    - Add `| 'fund'` (regulated fund vehicles), `| 'holding'` (purpose-built holding structures), `| 'pension'` (retirement/pension schemes) to the EntityCategory union type.
    - Update the comment from "Australian tax entity classifications" to "tax entity classifications" (it's now multi-jurisdiction).

    **3. Expand COLORS** in `src/lib/constants.ts`:
    - Add to `COLORS.entity` (BEFORE the `selected` and `error` entries):
      ```
      fund: '#8B5CF6',     // violet-500
      holding: '#0891B2',  // cyan-600
      pension: '#EA580C',  // orange-600
      ```
    - Add to `COLORS.entityBg`:
      ```
      fund: '#F5F3FF',     // violet-50
      holding: '#CFFAFE',  // cyan-100
      pension: '#FFF7ED',  // orange-50
      ```

    **4. Expand CATEGORY_CONFIG** in `src/lib/entity-registry.ts`:
    - Add 3 new entries to the `CATEGORY_CONFIG` array. Insert them in logical position:
      - `{ category: 'fund', label: 'Funds', iconName: 'landmark' }` — after 'partnership'
      - `{ category: 'holding', label: 'Holding Vehicles', iconName: 'layers' }` — after 'fund'
      - `{ category: 'pension', label: 'Pension Schemes', iconName: 'piggy-bank' }` — after 'smsf' (at end)
    - Update the file comment from "all Australian tax entity types" to "all tax entity types".

    **5. Expand PALETTE_ICONS** in `src/lib/palette-icons.ts`:
    - Import `Layers` and `PiggyBank` from `lucide-react` (add to existing import).
    - Add entries: `'layers': Layers` and `'piggy-bank': PiggyBank` to the `PALETTE_ICONS` record.
  </action>
  <verify>
    Run `npx tsc --noEmit` — should compile with zero errors. The expanded types must be consistent: every value in EntityCategory has a corresponding entry in COLORS.entity, COLORS.entityBg, and CATEGORY_CONFIG.
  </verify>
  <done>
    Jurisdiction type is a 6-member union. JURISDICTIONS has 6 entries with flags. EntityCategory has 9 values. COLORS has 9 entity colors + 9 entity backgrounds. CATEGORY_CONFIG has 9 entries. PALETTE_ICONS has entries for 'layers' and 'piggy-bank'. TypeScript compiles cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add 9 UK and 11 US entity registry entries</name>
  <files>
    src/lib/entity-registry.ts
  </files>
  <action>
    Add 20 new entries to `ENTITY_REGISTRY` in `src/lib/entity-registry.ts`, after the existing AU entries. Group UK entries together, then US entries. Use the exact entity data from the research:

    **UK (9 entries):**
    | ID | Category | Display Name | Short Name | Shape | Icon | Default Data |
    |----|----------|-------------|------------|-------|------|-------------|
    | `uk-ltd` | company | Private Limited Company | Ltd | rectangle | building-2 | `{ companyNumber: '', utr: '' }` |
    | `uk-plc` | company | Public Limited Company | PLC | rectangle | building-2 | `{ companyNumber: '', utr: '' }` |
    | `uk-llp` | partnership | Limited Liability Partnership | LLP | triangle | handshake | `{ companyNumber: '', utr: '' }` |
    | `uk-lp` | partnership | Limited Partnership | LP | triangle | file-signature | `{ lpNumber: '', utr: '' }` |
    | `uk-gp` | partnership | General Partnership | GP | triangle | handshake | `{ utr: '' }` |
    | `uk-unit-trust` | trust | Unit Trust | Unit Trust | triangle | shield | `{ utr: '', trusteeName: '' }` |
    | `uk-discretionary-trust` | trust | Discretionary Trust | Disc. Trust | triangle | shield-check | `{ utr: '', trusteeName: '' }` |
    | `uk-individual` | individual | Individual | Individual | oval | user | `{ nino: '', utr: '' }` |
    | `uk-pension-scheme` | pension | Pension Scheme | Pension | shield | piggy-bank | `{ hmrcReference: '' }` |

    **US (11 entries):**
    | ID | Category | Display Name | Short Name | Shape | Icon | Default Data |
    |----|----------|-------------|------------|-------|------|-------------|
    | `us-c-corp` | company | C Corporation | C Corp | rectangle | building-2 | `{ ein: '', stateOfFormation: '' }` |
    | `us-s-corp` | company | S Corporation | S Corp | rectangle | building-2 | `{ ein: '', stateOfFormation: '', sCorpElection: false }` |
    | `us-llc-disregarded` | company | LLC (Disregarded Entity) | LLC (DE) | rounded | building-2 | `{ ein: '', stateOfFormation: '' }` |
    | `us-llc-partnership` | partnership | LLC (Partnership-Taxed) | LLC (P) | triangle | handshake | `{ ein: '', stateOfFormation: '' }` |
    | `us-gp` | partnership | General Partnership | GP | triangle | handshake | `{ ein: '' }` |
    | `us-lp` | partnership | Limited Partnership | LP | triangle | file-signature | `{ ein: '', stateOfFormation: '' }` |
    | `us-lllp` | partnership | Limited Liability Limited Partnership | LLLP | triangle | file-signature | `{ ein: '', stateOfFormation: '' }` |
    | `us-grantor-trust` | trust | Grantor Trust | Grantor Trust | triangle | shield | `{ ein: '', trusteeName: '' }` |
    | `us-non-grantor-trust` | trust | Non-Grantor Trust | Non-Grantor Trust | triangle | shield-check | `{ ein: '', trusteeName: '' }` |
    | `us-individual` | individual | Individual | Individual | oval | user | `{ ssn: '', taxResidency: 'US' }` |
    | `us-501c3` | company | Tax-Exempt Organization | 501(c)(3) | hexagon | landmark | `{ ein: '', exemptionType: '501(c)(3)' }` |

    Each entry follows the same `EntityTypeConfig` structure as existing AU entries:
    - `id`: the key string
    - `jurisdiction`: `'UK'` or `'US'`
    - `category`: one of the EntityCategory values
    - `displayName`, `shortName`: from table above
    - `description`: one-line plain-English description of the entity type in the context of tax structuring (e.g., "A standard UK private company limited by shares under the Companies Act 2006.")
    - `shape`, `icon`: from table above
    - `color`: use `COLORS.entity.[category]` (e.g., `COLORS.entity.company` for company category, `COLORS.entity.pension` for pension)
    - `defaultData`: from table above
  </action>
  <verify>
    Run `npx tsc --noEmit` — zero errors. Then verify counts in a node script or by reading the file:
    - `getEntitiesByJurisdiction('UK').length === 9`
    - `getEntitiesByJurisdiction('US').length === 11`
    - `getEntitiesByJurisdiction('AU').length === 11` (unchanged)
  </verify>
  <done>
    ENTITY_REGISTRY contains 31 entries total (11 AU + 9 UK + 11 US). All UK and US entries have correct category assignments matching the research entity lists. TypeScript compiles cleanly.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `getEntitiesByJurisdiction('UK').length === 9`
3. `getEntitiesByJurisdiction('US').length === 11`
4. `getEntitiesByJurisdiction('AU').length === 11` (no regression)
5. Every new EntityCategory value has matching entries in COLORS.entity, COLORS.entityBg, and CATEGORY_CONFIG
6. PALETTE_ICONS has 'layers' and 'piggy-bank' entries
</verification>

<success_criteria>
- TypeScript compiles with zero errors after all changes
- Jurisdiction type is a 6-member union with JURISDICTIONS having all 6 configs
- EntityCategory has 9 values (6 existing + 3 new)
- ENTITY_REGISTRY has 31 entries (11 AU + 9 UK + 11 US)
- CATEGORY_CONFIG has 9 entries
- COLORS.entity and COLORS.entityBg each have 9 category entries
- PALETTE_ICONS has 12 entries (10 existing + 2 new)
</success_criteria>

<output>
After completion, create `.planning/phases/17-data-model-entity-registry/17-01-SUMMARY.md`
</output>
