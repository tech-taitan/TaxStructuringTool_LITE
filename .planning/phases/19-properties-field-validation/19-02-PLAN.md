---
phase: 19-properties-field-validation
plan: 02
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - src/components/properties/RegistrationSection.tsx
  - src/components/properties/TaxStatusSection.tsx
  - src/components/properties/IdentitySection.tsx
  - src/components/properties/ErrorSummary.tsx
autonomous: true

must_haves:
  truths:
    - "User opens a UK entity and sees Company Number and UTR fields in Registration, not ABN/ACN"
    - "User opens a US entity and sees EIN and State of Formation in Registration, not ABN/ACN"
    - "User opens HK/SG/LU entities and sees jurisdiction-appropriate registration fields"
    - "User opens a UK company and sees Corporation Tax Rate and Small Profits Rate in Tax Status, not Base Rate Entity"
    - "User opens a US LLC and sees Check-the-Box Election in Tax Status"
    - "User sees jurisdiction-correct tax residency options (e.g. 'UK Resident' not 'Australian Resident') for non-AU entities"
    - "User enters invalid registration format and sees inline validation error with human-readable field label"
    - "User sees 'United Kingdom' (not 'UK') in the Identity section jurisdiction display"
    - "Existing AU entities display identical Registration and Tax Status fields with no regressions"
  artifacts:
    - path: "src/components/properties/RegistrationSection.tsx"
      provides: "Jurisdiction-dispatched registration field rendering for all 6 jurisdictions"
      contains: "jurisdiction"
    - path: "src/components/properties/TaxStatusSection.tsx"
      provides: "Jurisdiction-dispatched tax status field rendering for all 6 jurisdictions"
      contains: "jurisdiction"
    - path: "src/components/properties/IdentitySection.tsx"
      provides: "JURISDICTIONS registry lookup for jurisdiction display name"
      contains: "JURISDICTIONS"
    - path: "src/components/properties/ErrorSummary.tsx"
      provides: "Expanded label map for all jurisdiction field paths"
      contains: "registration.companyNumber"
  key_links:
    - from: "src/components/properties/RegistrationSection.tsx"
      to: "getEntityConfig(entityType).jurisdiction"
      via: "jurisdiction extraction from entity config"
      pattern: "config\\?\\.jurisdiction"
    - from: "src/components/properties/TaxStatusSection.tsx"
      to: "getEntityConfig(entityType).jurisdiction"
      via: "jurisdiction extraction from entity config"
      pattern: "config\\?\\.jurisdiction"
    - from: "src/components/properties/IdentitySection.tsx"
      to: "JURISDICTIONS registry"
      via: "import and lookup for display name"
      pattern: "JURISDICTIONS\\["
---

<objective>
Refactor the properties panel UI to render jurisdiction-specific registration and tax status fields, fix the jurisdiction display name, and expand the error label map for all new fields.

Purpose: Users must see the correct fields when editing entities from any of the 6 jurisdictions. This plan adds the UI rendering that sits on top of the data model and schemas built in Plan 01.

Output: Jurisdiction-aware RegistrationSection, TaxStatusSection, IdentitySection, and ErrorSummary -- covering PROP-01 through PROP-06 requirements.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-properties-field-validation/19-RESEARCH.md
@.planning/phases/19-properties-field-validation/19-01-SUMMARY.md

@src/components/properties/RegistrationSection.tsx
@src/components/properties/TaxStatusSection.tsx
@src/components/properties/IdentitySection.tsx
@src/components/properties/ErrorSummary.tsx
@src/models/graph.ts
@src/lib/entity-registry.ts
@src/models/jurisdiction.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor RegistrationSection for jurisdiction-based dispatch and add all field renderers</name>
  <files>src/components/properties/RegistrationSection.tsx, src/components/properties/IdentitySection.tsx</files>
  <action>
**RegistrationSection.tsx -- Refactor dispatch and add jurisdiction renderers:**

1. Extract `jurisdiction` from entity config: `const jurisdiction = config?.jurisdiction ?? 'AU';`

2. Create a reusable `RegTextField` helper function (inside the component file, not exported) to avoid duplicating 10+ lines of JSX per text input:
```typescript
function RegTextField({ label, field, maxLength, value, onChange, error }: {
  label: string; field: string; maxLength?: number;
  value: string; onChange: (field: string, value: string) => void;
  error?: string;
}) {
  return (
    <div>
      <label className="block text-xs font-medium text-gray-500 mb-1">{label}</label>
      <input type="text" maxLength={maxLength} value={value}
        onChange={(e) => onChange(field, e.target.value)}
        className={`w-full px-3 py-1.5 text-sm border rounded-md outline-none focus:ring-1 focus:ring-blue-500 ${
          error ? 'border-red-400' : 'border-gray-300'
        }`} />
      {error && <p className="text-xs text-red-500 mt-1">{error}</p>}
    </div>
  );
}
```

3. Also create a `RegCheckbox` helper:
```typescript
function RegCheckbox({ label, field, checked, onChange }: {
  label: string; field: string; checked: boolean;
  onChange: (field: string, value: boolean) => void;
}) {
  return (
    <div className="flex items-center gap-2">
      <input type="checkbox" id={field} checked={checked}
        onChange={(e) => onChange(field, e.target.checked)}
        className="w-4 h-4 rounded border-gray-300" />
      <label htmlFor={field} className="text-sm text-gray-700">{label}</label>
    </div>
  );
}
```

4. Refactor `renderFields()` to dispatch on `jurisdiction` FIRST, then delegate to jurisdiction-specific render functions. Wrap existing AU cases inside a `renderAuFields(category)` function:

```typescript
const renderFields = () => {
  switch (jurisdiction) {
    case 'AU': return renderAuFields();
    case 'UK': return renderUkFields();
    case 'US': return renderUsFields();
    case 'HK': return renderHkFields();
    case 'SG': return renderSgFields();
    case 'LU': return renderLuFields();
    default: return null;
  }
};
```

5. `renderAuFields()` -- Move the existing `switch (category)` code verbatim into this function. Use the new helper components to reduce duplication if desired, but DO NOT change behavior. The existing AU rendering MUST remain identical.

6. `renderUkFields()` -- Render based on category:
   - `company` (uk-ltd, uk-plc): Company Number (maxLength=8), UTR (maxLength=10)
   - `partnership` (uk-llp, uk-lp, uk-gp): Company Number (for LLP only -- check entityType), LP Number (for LP only), UTR
   - `trust` (uk-unit-trust, uk-discretionary-trust): UTR
   - `individual` (uk-individual): NINO (maxLength=9), UTR (maxLength=10)
   - `pension` (uk-pension-scheme): HMRC Reference
   - For partnership subtype rendering, use `entityType` checks: if entityType is `uk-llp` show Company Number + UTR, if `uk-lp` show LP Number + UTR, if `uk-gp` show UTR only

7. `renderUsFields()` -- Render based on category:
   - `company` (us-c-corp, us-s-corp, us-501c3): EIN (maxLength=10, placeholder "XX-XXXXXXX"), State of Formation dropdown
   - `partnership` (us-gp, us-lp, us-lllp, us-llc-partnership, us-llc-disregarded): EIN, State of Formation dropdown
   - `trust` (us-grantor-trust, us-non-grantor-trust): EIN
   - `individual` (us-individual): SSN/ITIN (maxLength=11, placeholder "XXX-XX-XXXX")
   - State of Formation dropdown: Create a `US_STATES` constant array with all 50 states + DC (code/name pairs). Render as a `<select>` with "Select..." default option. Place the array at module scope in the same file.

8. `renderHkFields()` -- Render based on category:
   - `company` (hk-private-co, hk-public-co): CR Number (maxLength=8), BRN (maxLength=8)
   - `partnership` (hk-lp): BRN
   - `fund` (hk-lpf, hk-ofc): BRN
   - `individual` (hk-individual): HKID

9. `renderSgFields()` -- Render based on category:
   - `company` (sg-pte-ltd, sg-public-co): UEN (maxLength=10)
   - `partnership` (sg-lp, sg-llp): UEN
   - `fund` (sg-vcc): UEN
   - `trust` (sg-unit-trust): UEN
   - `individual` (sg-individual): NRIC/FIN (maxLength=9)

10. `renderLuFields()` -- Render based on category:
    - `company` (lu-sarl, lu-sa, lu-soparfi): RCS Number
    - `partnership` (lu-scsp, lu-scs): RCS Number
    - `fund` (lu-sicav, lu-sicar, lu-sif, lu-raif): RCS Number, CSSF Approval Number (only for lu-sicav, lu-sicar, lu-sif -- check entityType)
    - `individual` (lu-individual): Tax ID / Matricule (maxLength=13)

For ALL text fields, pass the `error` prop from `errors[`registration.${fieldName}`]`.

**IdentitySection.tsx -- Fix jurisdiction display:**

1. Add import: `import { JURISDICTIONS, type Jurisdiction } from '@/models/jurisdiction';`
2. Replace the hardcoded jurisdiction display (line 173):
   - Old: `{formData.jurisdiction === 'AU' ? 'Australia' : formData.jurisdiction}`
   - New: `{JURISDICTIONS[formData.jurisdiction as Jurisdiction]?.name ?? formData.jurisdiction}`
  </action>
  <verify>Run `npx tsc --noEmit` -- zero errors. Start dev server, place a UK Ltd entity, open properties, expand Registration section -- should show "Company Number" and "UTR" fields (not ABN/ACN). Place an AU Pty Ltd -- should still show ABN and ACN. Place a US C Corp -- should show EIN and State of Formation. Check that the Identity section shows "United Kingdom" for a UK entity.</verify>
  <done>RegistrationSection dispatches on jurisdiction, showing correct fields for all 6 jurisdictions. IdentitySection shows full jurisdiction names from JURISDICTIONS registry. AU entities unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor TaxStatusSection for jurisdiction-based dispatch and expand ErrorSummary labels</name>
  <files>src/components/properties/TaxStatusSection.tsx, src/components/properties/ErrorSummary.tsx</files>
  <action>
**TaxStatusSection.tsx -- Refactor dispatch and add jurisdiction renderers:**

1. Extract `jurisdiction` from entity config: `const jurisdiction = config?.jurisdiction ?? 'AU';`

2. Create helper components (same pattern as RegistrationSection):
   - `TaxCheckbox` for boolean tax status fields
   - `TaxNumberField` for numeric rate fields (already has the % conversion pattern from existing code -- reuse it)
   - `TaxSelectField` for dropdown fields

3. Refactor `renderTaxResidency()` to accept a `jurisdiction` parameter and return jurisdiction-appropriate options:
   - AU: "Australian Resident" / "Foreign Resident" (existing values unchanged)
   - UK: "UK Resident" / "Non-Resident"
   - US: "US Resident" / "Non-Resident Alien"
   - HK: "HK Resident" / "Non-Resident"
   - SG: "SG Resident" / "Non-Resident"
   - LU: "LU Resident" / "Non-Resident"

4. Refactor `renderFields()` to dispatch on `jurisdiction` first:
```typescript
const renderFields = () => {
  switch (jurisdiction) {
    case 'AU': return renderAuFields();
    case 'UK': return renderUkFields();
    case 'US': return renderUsFields();
    case 'HK': return renderHkFields();
    case 'SG': return renderSgFields();
    case 'LU': return renderLuFields();
    default: return null;
  }
};
```

5. `renderAuFields()` -- Move existing `switch (category)` code verbatim. MUST be identical to current behavior.

6. `renderUkFields()` based on category:
   - `company` (uk-ltd, uk-plc): Corporation Tax Rate (number %, same pattern as existing Tax Rate field), Small Profits Rate (checkbox), Tax Residency
   - `partnership` (uk-llp, uk-lp, uk-gp): Tax Residency only
   - `trust` (uk-unit-trust, uk-discretionary-trust): Display info text "Trust rate: 45% (standard/dividend)" as a `<p>` tag, Tax Residency
   - `individual` (uk-individual): IHT Relevant (checkbox), Tax Residency
   - `pension` (uk-pension-scheme): Tax Residency only

7. `renderUsFields()` based on category:
   - `company` (us-c-corp, us-s-corp): S Corp Election checkbox (only show if entityType is `us-s-corp`), Federal Tax Rate (number %), Tax Residency
   - `partnership` (us-llc-disregarded, us-llc-partnership): Check-the-Box Election dropdown (options: "", "Disregarded Entity", "Partnership", "Corporation"), Tax Residency. For us-gp, us-lp, us-lllp: Tax Residency only.
   - `trust` (us-grantor-trust, us-non-grantor-trust): Tax Residency only
   - `individual` (us-individual): Tax Residency only
   - `company` for us-501c3: Tax Residency only (tax-exempt)

8. `renderHkFields()` based on category:
   - `company` (hk-private-co, hk-public-co): Two-Tier Profits Tax (checkbox), display info text "Territorial source principle applies", Tax Residency
   - `partnership` (hk-lp): display info text "Territorial source principle applies", Tax Residency
   - `fund` (hk-lpf, hk-ofc): display info text "Territorial source principle applies", Tax Residency
   - `individual` (hk-individual): Tax Residency only

9. `renderSgFields()` based on category:
   - `company` (sg-pte-ltd, sg-public-co): Partial Tax Exemption (checkbox), Tax Residency
   - `fund` (sg-vcc): Section 13O/13U Election dropdown (options: "", "Section 13O", "Section 13U"), VCC Sub-Fund Structure (checkbox), Tax Residency
   - `trust` (sg-unit-trust): Section 13O/13U Election dropdown, Tax Residency
   - `partnership` (sg-lp, sg-llp): Tax Residency only
   - `individual` (sg-individual): Tax Residency only

10. `renderLuFields()` based on category:
    - `company` for lu-soparfi: SOPARFI Flag (checkbox, default true -- show as read-only checked), Participation Exemption (checkbox), Tax Residency
    - `company` (lu-sarl, lu-sa): Participation Exemption (checkbox), IP Box Election (checkbox), Tax Residency
    - `fund` (lu-sicav, lu-sicar, lu-sif): Subscription Tax Rate (number %), Tax Residency
    - `partnership` (lu-scsp, lu-scs): Tax Residency only
    - `fund` for lu-raif: Tax Residency only
    - `individual` (lu-individual): Tax Residency only

For ALL numeric rate fields, use the same pattern as existing `taxRate` field: display as percentage (value * 100), store as decimal (parseFloat / 100). Use the field-specific name (e.g., `corporationTaxRate`, `federalTaxRate`, `subscriptionTaxRate`).

**ErrorSummary.tsx -- Expand labelMap:**

Add ALL new field path labels to the `labelMap` in `pathToLabel()`:

Registration fields:
- `'registration.companyNumber': 'Company Number'`
- `'registration.utr': 'UTR'`
- `'registration.nino': 'NINO'`
- `'registration.lpNumber': 'LP Number'`
- `'registration.hmrcReference': 'HMRC Reference'`
- `'registration.ein': 'EIN'`
- `'registration.ssn': 'SSN/ITIN'`
- `'registration.stateOfFormation': 'State of Formation'`
- `'registration.crNumber': 'CR Number'`
- `'registration.brn': 'Business Registration Number'`
- `'registration.hkid': 'HKID'`
- `'registration.uen': 'UEN'`
- `'registration.nric': 'NRIC/FIN'`
- `'registration.rcsNumber': 'RCS Number'`
- `'registration.cssfApprovalNumber': 'CSSF Approval Number'`
- `'registration.nationalId': 'Tax ID (Matricule)'`

Tax status fields:
- `'taxStatus.corporationTaxRate': 'Corporation Tax Rate'`
- `'taxStatus.smallProfitsRate': 'Small Profits Rate'`
- `'taxStatus.ihtRelevant': 'IHT Relevant'`
- `'taxStatus.checkTheBoxElection': 'Check-the-Box Election'`
- `'taxStatus.sCorpElection': 'S Corp Election'`
- `'taxStatus.federalTaxRate': 'Federal Tax Rate'`
- `'taxStatus.twoTierProfitsTax': 'Two-Tier Profits Tax'`
- `'taxStatus.section13Election': 'Section 13O/13U Election'`
- `'taxStatus.partialTaxExemption': 'Partial Tax Exemption'`
- `'taxStatus.vccSubFundStructure': 'VCC Sub-Fund Structure'`
- `'taxStatus.soparfiFlag': 'SOPARFI'`
- `'taxStatus.participationExemption': 'Participation Exemption'`
- `'taxStatus.subscriptionTaxRate': 'Subscription Tax Rate'`
- `'taxStatus.ipBoxElection': 'IP Box Election'`
  </action>
  <verify>Run `npx tsc --noEmit` -- zero errors. Start dev server and test:
1. Place a UK Ltd, open properties, expand Tax Status -- should show "Corporation Tax Rate", "Small Profits Rate", and Tax Residency with "UK Resident"/"Non-Resident" options (NOT "Australian Resident")
2. Place a US LLC Partnership, expand Tax Status -- should show "Check-the-Box Election" dropdown
3. Place an AU Pty Ltd, expand Tax Status -- should show "Base Rate Entity" checkbox and "Tax Rate" field (unchanged)
4. Enter an invalid UK Company Number (e.g. "ABC"), check ErrorSummary shows "Company Number: Company Number must be 8 alphanumeric characters" (not raw "companyNumber")
</verify>
  <done>TaxStatusSection dispatches on jurisdiction, showing correct fields for all 6 jurisdictions with jurisdiction-aware tax residency dropdowns. ErrorSummary displays human-readable labels for all new fields. AU entities unchanged.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. UK entity properties show Company Number, UTR, Corporation Tax Rate, Small Profits Rate, and "UK Resident"/"Non-Resident" residency options
3. US entity properties show EIN, State of Formation, and correct tax status fields per entity type
4. HK entity properties show CR Number, BRN, Two-Tier Profits Tax
5. SG entity properties show UEN, Section 13O/13U, Partial Tax Exemption
6. LU entity properties show RCS Number, SOPARFI flag, Participation Exemption, Subscription Tax Rate
7. Invalid registration number format shows inline error with human-readable label in ErrorSummary
8. AU entity properties are identical to pre-phase behavior
9. Identity section shows full jurisdiction name (e.g., "United Kingdom") for all jurisdictions
10. Mobile properties sheet renders the same fields (flows through automatically)
</verification>

<success_criteria>
- RegistrationSection and TaxStatusSection dispatch on jurisdiction first, then category
- All 6 jurisdictions render their correct registration and tax status fields
- Tax residency dropdowns show jurisdiction-appropriate options
- ErrorSummary displays human-readable labels for all new field paths
- IdentitySection shows full country names from JURISDICTIONS registry
- AU entity properties have zero visual or behavioral regressions
- Requirements PROP-01 through PROP-06 are fully covered
</success_criteria>

<output>
After completion, create `.planning/phases/19-properties-field-validation/19-02-SUMMARY.md`
</output>
