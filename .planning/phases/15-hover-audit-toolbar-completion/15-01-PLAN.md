---
phase: 15-hover-audit-toolbar-completion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/mobile/MobileOverflowMenu.tsx
  - src/components/editor/MobileEditorLayout.tsx
  - src/components/editor/EditorLayout.tsx
autonomous: true

must_haves:
  truths:
    - "User can access save, templates, auto-layout, and export via an overflow menu in the mobile toolbar"
    - "User can access undo, redo, add, connect, and analyze tools via the mobile bottom toolbar"
    - "Overflow menu closes when tapping outside it"
  artifacts:
    - path: "src/components/mobile/MobileOverflowMenu.tsx"
      provides: "Overflow menu component with save, templates, auto-layout, export actions"
      min_lines: 40
    - path: "src/components/editor/MobileEditorLayout.tsx"
      provides: "Mobile toolbar with overflow menu integrated"
      contains: "MobileOverflowMenu"
    - path: "src/components/editor/EditorLayout.tsx"
      provides: "Props threading from EditorLayout to MobileEditorLayout"
      contains: "onAutoLayout"
  key_links:
    - from: "src/components/editor/EditorLayout.tsx"
      to: "src/components/editor/MobileEditorLayout.tsx"
      via: "props: onAutoLayout, onShowTemplates, onExportPng"
      pattern: "onAutoLayout|onShowTemplates|onExportPng"
    - from: "src/components/editor/MobileEditorLayout.tsx"
      to: "src/components/mobile/MobileOverflowMenu.tsx"
      via: "import and render MobileOverflowMenu with action props"
      pattern: "MobileOverflowMenu"
---

<objective>
Complete the mobile bottom toolbar by adding a MobileOverflowMenu component that surfaces save, templates, auto-layout, and export actions -- the missing toolbar features identified by requirements MNAV-01, MNAV-02, and MNAV-03.

Purpose: The mobile toolbar currently has undo, redo, add, connect, and analyze buttons, but lacks save, templates, auto-layout, and export. These are available on desktop via EditorToolbar but not accessible on phone. The overflow menu provides a clean "..." button that opens a positioned popover with these secondary actions.

Output: MobileOverflowMenu component, updated MobileEditorLayout with overflow button, updated EditorLayout with prop threading.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/editor/MobileEditorLayout.tsx
@src/components/editor/EditorLayout.tsx
@src/components/toolbar/EditorToolbar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MobileOverflowMenu and wire into MobileEditorLayout toolbar</name>
  <files>
    src/components/mobile/MobileOverflowMenu.tsx
    src/components/editor/MobileEditorLayout.tsx
    src/components/editor/EditorLayout.tsx
  </files>
  <action>
**1. Create `src/components/mobile/MobileOverflowMenu.tsx`:**

A self-contained overflow menu component that renders:
- A "..." trigger button (`MoreHorizontal` icon from lucide-react, 20x20, `p-2.5 rounded-full`)
- When open: an absolutely-positioned popover (`absolute bottom-full mb-2 right-0`) with:
  - **Save** button (Save icon) -- disabled when `!canSave || isSaving`, shows Loader2 spinner when saving
  - **Templates** button (BookOpen icon) -- always enabled
  - **Auto-Layout** button (LayoutGrid icon) -- always enabled
  - **Export PNG** button (Download icon) -- only rendered if `onExportPng` is provided

Props interface `MobileOverflowMenuProps`:
```ts
interface MobileOverflowMenuProps {
  onSave?: () => void;
  isSaving?: boolean;
  canSave?: boolean;
  onAutoLayout: () => void;
  onShowTemplates: () => void;
  onExportPng?: () => void;
}
```

Close-on-tap-outside: Use `useEffect` with `pointerdown` event listener (NOT `mousedown` -- `pointerdown` handles both mouse and touch). Check `menuRef.current.contains(e.target)` and close if outside.

Each menu item: `w-full px-4 py-3 text-sm text-left active:bg-gray-100 flex items-center gap-3`. After any action click, also call `setIsOpen(false)`.

Popover container: `min-w-[180px] bg-white rounded-lg shadow-lg border border-gray-200 py-1 z-50`.

**2. Update `MobileEditorLayout.tsx`:**

Add new props to `MobileEditorLayoutProps`:
- `onAutoLayout: () => void`
- `onShowTemplates: () => void`
- `onExportPng?: () => void`

Destructure these new props in the component function.

Import `MobileOverflowMenu` from `@/components/mobile/MobileOverflowMenu`.

In the toolbar (the `absolute bottom-4` div), add after the Analyze button and its divider:
- Another divider (`<div className="w-px h-6 bg-gray-200 dark:bg-gray-600 mx-1" />`)
- `<MobileOverflowMenu>` with all the threaded props: `onSave`, `isSaving`, `canSave`, `onAutoLayout`, `onShowTemplates`, `onExportPng`.

**3. Update `EditorLayout.tsx`:**

In the `EditorLayoutInner` function where `<MobileEditorLayout>` is rendered (around line 263-276), add the missing props:
- `onAutoLayout={handleAutoLayout}`
- `onShowTemplates={() => setShowTemplatePicker(true)}`
- `onExportPng={handleExportPng}`

These handlers already exist in EditorLayoutInner -- just thread them down.
  </action>
  <verify>
1. `npx tsc --noEmit` passes with no type errors
2. Open the app at mobile viewport (<768px) in browser dev tools
3. Bottom toolbar shows all existing buttons PLUS a "..." overflow button at the right end
4. Tapping "..." opens a popover above the toolbar with Save, Templates, Auto-Layout, Export PNG
5. Tapping outside the popover closes it
6. Tapping any menu item triggers the action and closes the popover
  </verify>
  <done>
Mobile toolbar has a "..." overflow button that opens a popover with Save, Templates, Auto-Layout, and Export PNG actions. All actions are wired to the same handlers used by the desktop EditorToolbar. The overflow menu closes on tap-outside using pointerdown events (MNAV-01, MNAV-02, MNAV-03 satisfied).
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` -- no type errors
2. Mobile viewport (<768px): toolbar shows undo, redo, add, connect, analyze, and "..." overflow
3. Overflow menu items: Save (respects canSave/isSaving), Templates, Auto-Layout, Export PNG
4. Templates action opens the same TemplatePickerModal used on desktop
5. Auto-Layout action arranges entities hierarchically
6. Save action saves the structure (if save is available)
7. Export PNG exports the canvas as PNG
8. Desktop viewport (>1024px): completely unchanged
</verification>

<success_criteria>
- MobileOverflowMenu component exists and renders a popover with 4 actions
- MobileEditorLayout toolbar includes the overflow button
- EditorLayout threads onAutoLayout, onShowTemplates, onExportPng to MobileEditorLayout
- All actions work correctly and popover closes after selection or tap-outside
- No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/15-hover-audit-toolbar-completion/15-01-SUMMARY.md`
</output>
